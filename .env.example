# Postgres
# On Vercel+Neon:
# - DATABASE_URL should be the pooled URL (with pgbouncer=true&connection_limit=1)
# - DIRECT_URL should be the direct/unpooled URL for migrations
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require&pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# App
NODE_ENV="development"

# LLM
# If OPENAI_API_KEY is empty, the app uses a safe mock responder.
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4.1-mini"

# Security
SESSION_DAYS="14"

# AI Provider
# node (default) uses OpenAI directly from Next.js
# python uses external FastAPI service
AI_PROVIDER=node

# Python AI service (V2.1)
PY_AI_URL=
PY_AI_SECRET=

# Optional extractor (profile patch) after medical/therapy chat (1 = enabled, 0 = disabled)
PROFILE_EXTRACTOR_ENABLED=1

# Recipes cache TTL in seconds (default 3600 = 1 hour)
RECIPE_CACHE_TTL_SECONDS=3600
