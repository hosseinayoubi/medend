import ChatScreen from "@/components/chat/ChatScreen";

export default function DentalChatPage() {
  return (
    <ChatScreen
      mode="dental"
      title="Dental Chat"
      subtitle="Tooth pain, gum issues, sensitivity, swelling. Not a diagnosis."
    />
  );
}
